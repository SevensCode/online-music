import React from 'react';
import './index.less';

const MusicPlayer = () => {
    return (
        <div className={'musicPlayer gaussianBlur'}>
            <div className="musicPlayer-container"></div>
        </div>
    );
};

export default MusicPlayer;
